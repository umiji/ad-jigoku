{
  "name": "@ad-jigoku/game-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ヘッドレス・純粋・決定論的なゲームコア（DOM / React / Date / Math.random / setTimeout 禁止 / ADR-002）",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "lint": "pnpm -w exec eslint packages/game-engine",
    "test": "vitest run",
    "game:preview-stage": "tsx bin/preview-stage.ts"
  },
  "dependencies": {
    "@ad-jigoku/pattern-catalog": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "eslint": "^9.39.5",
    "typescript": "^5.9.3",
    "vitest": "^3.2.7",
    "tsx": "^4.23.13"
  }
}
